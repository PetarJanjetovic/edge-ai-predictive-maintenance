# Resume Material

Replace bracketed values only after measuring them on physical hardware.

## Project entry

**Edge AI Predictive Maintenance System** | ESP32-S3, C++, Python, scikit-learn, FastAPI

- Developed an embedded vibration-monitoring device that classified normal operation, imbalance, misalignment, and bearing faults locally on an ESP32-S3 with `[accuracy]%` held-out accuracy.
- Implemented 1 kHz sensor acquisition, statistical and frequency-domain feature extraction, and neural-network inference in C++, achieving `[latency] ms` latency with `[memory] KB` model memory.
- Built a real-time telemetry dashboard and fault-alert pipeline, measuring a `[false-positive]%` false-alarm rate across `[duration]` minutes of continuous operation.

## Repository checklist

- Add a clear hardware photograph to the README.
- Add a 30-60 second demonstration video.
- Commit the real-data confusion matrix and benchmark table.
- Document failures and model iterations in the project report.
- Tag the final demonstrated release.

